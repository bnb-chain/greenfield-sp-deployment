bases:
  - ../../base

namespace: greenfield-sp

nameSuffix: -a

commonLabels:
    team: greenfield
    biz: storage-provider
    app: syncer
    cluster_id: a

configMapGenerator:
- name: config
  files:
  - config/downloader_challenge.toml
  - config/gateway.toml
  - config/stonehub.toml
  - config/stonenode.toml
  - config/syncer.toml
  - config/uploader.toml

